# Wallet API Service

REST API сервис для управления кошельками с операциями пополнения (DEPOSIT) и снятия (WITHDRAW) средств.

## Содержание

- [Технологии](#технологии)
- [Архитектура](#архитектура)
- [Быстрый старт](#быстрый-старт)
- [API Endpoints](#api-endpoints)
- [Конфигурация](#конфигурация)
- [Тестирование](#тестирование)
- [Нагрузочное тестирование](#нагрузочное-тестирование)
- [Разработка](#разработка)
- [Примеры использования](#примеры-использования)

## Технологии

- **Go 1.25**
- **Fiber** - быстрый HTTP фреймворк
- **PostgreSQL** - база данных
- **GORM** - ORM для работы с БД
- **Docker & Docker Compose** - контейнеризация
- **Goose** - миграции БД
- **Testify** - тестирование

## Архитектура

Проект следует принципам **Clean Architecture** с разделением на слои:

```
internal/
├── handler/      # HTTP handlers (Presentation Layer)
├── service/      # Бизнес-логика (Business Logic Layer)
├── repository/   # Работа с БД (Data Access Layer)
├── models/       # Модели данных
├── database/     # Конфигурация БД
├── server/       # Настройка HTTP сервера
└── errors/       # Кастомные ошибки
```

### Ключевые особенности:

- **Graceful shutdown**: корректное завершение работы сервера
- **Dependency injection**: явное управление зависимостями
- **Comprehensive testing**: покрытие тестами handler и service слоев

## Быстрый старт

### Требования

- Go 1.25+
- Docker & Docker Compose
- Make
- [Goose](https://github.com/pressly/goose) для миграций (опционально)
- [wrk](https://github.com/wg/wrk) для тестирования RPS (опционально)

### Установка и запуск

1. **Клонирование репозитория**
```bash
git clone <repository-url>
cd itk-academy-test-case
```

2. **Запуск всех сервисов**
```bash
make start
```

Эта команда:
- Соберет Docker образы
- Запустит PostgreSQL и приложение
- Выполнит миграции БД

3. **Проверка работы**
```bash
curl http://localhost:8080/api/v1/wallets/73fd60a4-ef54-484c-9de8-4beb3808da26
```

### Остановка

```bash
make stop
```

## API Endpoints

### 1. Получить баланс кошелька

```http
GET /api/v1/wallets/{WALLET_UUID}
```

**Успешный ответ (200 OK):**
```json
{
    "WalletUUID": "73fd60a4-ef54-484c-9de8-4beb3808da26",
    "Balance": 10000,
    "CreatedAt": "2025-12-06T12:43:07.328138Z",
    "UpdatedAt": "2025-12-06T13:00:46.122202Z"
}
```

**Ошибки:**
- `400 Bad Request` - невалидный UUID
- `404 Not Found` - кошелек не найден

### 2. Операция с кошельком

```http
POST /api/v1/wallet
Content-Type: application/json

{
  "wallet_id": "73fd60a4-ef54-484c-9de8-4beb3808da26",
  "operation_type": "DEPOSIT",
  "amount": 1000
}
```

**Параметры:**
- `wallet_id` (UUID) - идентификатор кошелька
- `operation_type` (string) - тип операции: `DEPOSIT` или `WITHDRAW`
- `amount` (int) - сумма операции (должна быть > 0)

**Успешный ответ (200 OK):**
```json
{
    "WalletUUID": "73fd60a4-ef54-484c-9de8-4beb3808da26",
    "Balance": 10000,
    "CreatedAt": "2025-12-06T12:05:18.725945Z",
    "UpdatedAt": "2025-12-06T12:26:36.18786Z"
}
```

**Ошибки:**
- `400 Bad Request` - невалидные данные, отрицательная сумма, неизвестный тип операции
- `404 Not Found` - кошелек не найден
- `500 Internal Server Error` - ошибка сервера

### OpenAPI спецификация

Полная документация API доступна в файле `specs/openapi.yaml`

## Конфигурация

Переменные окружения настраиваются в `.env`:

```env
SERVER_HOST=0.0.0.0
SERVER_PORT=8080
DATABASE_HOST=database
DATABASE_PORT=5432
DATABASE_USER=dev
DATABASE_PASSWORD=dev
DATABASE_DBNAME=dev
DATABASE_SSLMODE=disable
```

### Локальная разработка

Для запуска без Docker:

1. Запустите PostgreSQL
2. Настройте переменные окружения
3. Выполните миграции:
```bash
make migrations-up
```
4. Запустите приложение:
```bash
make local-start
```

## 🧪 Тестирование

### Unit тесты

```bash
# Запустить все тесты
make test

# С покрытием кода
make test-cover
```

### Примеры тестов

```bash
# Тесты service слоя
go test ./internal/service -v

# Тесты handler слоя
go test ./internal/handler -v
```

## Нагрузочное тестирование

Проект включает настройку для нагрузочного тестирования с помощью [wrk](https://github.com/wg/wrk).

### Установка wrk

```bash
# macOS
brew install wrk

# Ubuntu
sudo apt-get install wrk
```

### Запуск тестов

```bash
make test-rps
```

Эта команда выполнит:
1. POST запросы (30 сек, 12 потоков, 400 соединений)
2. GET запросы (30 сек, 12 потоков, 400 соединений)

### Результаты производительности

```
POST /api/v1/wallet
- RPS: ~2,200 req/sec
- Latency avg: ~183ms
- Success rate: 100%
- Max latency: 1s

GET /api/v1/wallets/{UUID}
- RPS: ~26,500 req/sec
- Latency avg: ~17ms
- Success rate: 100%
```

### Кастомное тестирование

```bash
# Измените параметры в makefile
wrk -t12 -c600 -d60s -s ./post.lua http://localhost:8080/api/v1/wallet
```

## Разработка

### Создание миграции

```bash
make create-migration
# Введите имя миграции: add_user_table
```

Миграция будет создана в `db/migrations/`

### Применение миграций

```bash
# Вверх
make migrations-up

# Вниз (откат)
make migrations-down
```

### Форматирование кода

```bash
make fmt
```

### Структура Makefile команд

```bash
# Миграции
make create-migration    # Создать новую миграцию
make migrations-up       # Применить миграции
make migrations-down     # Откатить миграции

# Docker
make compose-build       # Собрать образы
make compose-up          # Запустить контейнеры
make compose-down        # Остановить контейнеры

# Сервис
make start               # Полный запуск (build + up + migrations)
make stop                # Полная остановка (migrations down + compose down)
make local-start         # Локальный запуск без Docker

# Тестирование
make test                # Запустить тесты
make test-cover          # Тесты с покрытием
make test-rps            # Нагрузочное тестирование

# Форматирование
make fmt                 # Форматировать код
```

## Примеры использования

### cURL примеры

**Пополнение кошелька:**
```bash
curl -X POST http://localhost:8080/api/v1/wallet \
  -H "Content-Type: application/json" \
  -d '{
    "wallet_id": "73fd60a4-ef54-484c-9de8-4beb3808da26",
    "operation_type": "DEPOSIT",
    "amount": 1000
  }'
```

**Снятие средств:**
```bash
curl -X POST http://localhost:8080/api/v1/wallet \
  -H "Content-Type: application/json" \
  -d '{
    "wallet_id": "73fd60a4-ef54-484c-9de8-4beb3808da26",
    "operation_type": "WITHDRAW",
    "amount": 500
  }'
```

**Проверка баланса:**
```bash
curl http://localhost:8080/api/v1/wallets/73fd60a4-ef54-484c-9de8-4beb3808da26
```

**Создано с использованием:** Go + Fiber + PostgreSQL + Docker